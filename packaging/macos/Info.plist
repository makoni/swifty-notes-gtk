<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string></string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleDisplayName</key>
	<string>Swifty Notes</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSHumanReadableCopyright</key>
	<string></string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<!--
	libadwaita aborts at startup with `No GSettings schemas are installed`
	unless GLib finds the Homebrew-compiled schemas. Launch Services injects
	these env vars when the bundle is double-clicked, run via `open .app`,
	or launched by Xcode through Launch Services. They are NOT applied when
	the binary is exec'd directly from a terminal — see CONTRIBUTING for
	the manual XDG_DATA_DIRS export developers need for that path.

	For a self-contained distribution bundle (vendored dylibs, signed,
	notarized), point XDG_DATA_DIRS at `@executable_path/../Resources` and
	copy `glib-2.0/schemas/gschemas.compiled` into Resources via a Build
	Phase. See packaging/macos/README.md for the recipe.
	-->
	<key>LSEnvironment</key>
	<dict>
		<key>XDG_DATA_DIRS</key>
		<string>/opt/homebrew/share</string>
	</dict>
</dict>
</plist>
